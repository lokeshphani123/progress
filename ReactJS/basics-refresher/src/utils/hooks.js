const hooksDefinitions = [{
    title: "useState: ",
    definition: "This state is all about registering variables. They are handled by React and are updated by the function that's provided by React, that will also tell react that data changed and finally that will cause React to update the UI. The special function is called 'useState' and this is also called React hook. In other words, It's data that, when changed, causes React to re-evalute a component.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state. <br/> 3. Compute values are values that shouldn't be managed as separate state since they can be derived from other state."
    },
    {
    title: "useEffect: ",
    definition: "The useEffect Hook allows you to perform side effects in your components. Some examples of side effects are: fetching data, directly updating the DOM, and timers.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    },
    {
    title: "useContext: ",
    definition: "The useEffect Hook allows you to perform side effects in your components. Some examples of side effects are: fetching data, directly updating the DOM, and timers.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    },
    {
    title: "useRef: ",
    definition: "This state is all about registering variables. They are handled by React and are updated by the function that's provided by React, that will also tell react that data changed and finally that will cause React to update the UI. The special function is called 'useState' and this is also called React hook.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    },
    {
    title: "useReducer: ",
    definition: "The useEffect Hook allows you to perform side effects in your components. Some examples of side effects are: fetching data, directly updating the DOM, and timers.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    },
    {
    title: "useCallback: ",
    definition: "The useEffect Hook allows you to perform side effects in your components. Some examples of side effects are: fetching data, directly updating the DOM, and timers.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    },
    {
    title: "useMemo: ",
    definition: "The useEffect Hook allows you to perform side effects in your components. Some examples of side effects are: fetching data, directly updating the DOM, and timers.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    },
    {
    title: "Custom Hook: ",
    definition: "The useEffect Hook allows you to perform side effects in your components. Some examples of side effects are: fetching data, directly updating the DOM, and timers.",
    rules: "1. The hooks are only declared inside of Component funcations <br/> 2. The hooks should only be declared on the top level or nested in code.This is App state."
    }
];

export default hooksDefinitions;